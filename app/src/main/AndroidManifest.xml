<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zl.myappbasicframe">
    //读取手机状态
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".application.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".userpremissions.PermissionsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.SwipeRefreshLayoutActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.Widget_Notification_Activity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.PicUploadActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.EditTextDemo"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.PhotoWaterFallDemo"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.SenserListenerActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.PhotoWallDemo"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.HandlerTestActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.HandlerTest_two_Activity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.CameraDemoActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.BannerActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.IconFontActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.PictureClickActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.OrderSelectActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.PicCoordinateSystemActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activity.CustomControlActivity"
            android:label="@string/app_name" />
    </application>

</manifest>